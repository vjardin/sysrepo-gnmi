[pytest]
junit_suite_name = gNMI Compliance
junit_family = xunit2
filterwarnings =
    ignore::pytest.PytestExperimentalApiWarning
markers =
    timing_sensitive: Tests that depend on wall-clock timing (may flake under valgrind)
